using System.Collections.Generic;
using UnityEngine;

namespace Dune
{
    [CreateAssetMenu(
        menuName = "Dune/Architecture/Events/Game event",
        fileName = "GameEvent",
        order = 0)]
    public class GameEvent : ScriptableObject
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
